<div class="images">
  {% for image in images %}
  <p><img src="{{ image.standard_resolution.url }} " /></p>";
    <p><a href="{{ image.link }}"><img src="{{ image.images.standard_resolution.url }} " /></a></p>
  {% endfor %}
</div>
